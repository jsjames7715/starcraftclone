-- fighter.lua – simple air unit script
-- Handles basic flight (no ground collision) and laser attack.

local script = {}

function script.BeginFlying(unitID)
    -- Nothing special – Spring handles air movement automatically.
end

function script.Update(unitID)
    -- Placeholder: could add a fuel consumption mechanic here.
end

return script
